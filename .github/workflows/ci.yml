on:
  pull_request_target:

permissions: {}

jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
    - run: |
        echo "running pull request target"
  build2:
    runs-on: ubuntu-22.04
    steps:
    - env:
        TITLE: ${{ github.event.pull_request.title }}
      run: |
        echo The Title of your PR is "$TITLE"
